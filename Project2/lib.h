#pragma once
#include <iostream>
#include <string>
#include <fstream>

using namespace std;

void inputFile(const char* fileName); //введення текстового файлу
void printFile(const char*); //виведення текстового файлу
void rewriteFile(const char*, const char*); //редагування текстового файлу